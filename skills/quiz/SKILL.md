---
name: quiz
description: git diff でこのブランチの変更コードを確認して、私がコードを1行1行理解しているか確認するためのクイズを行ってください。
---

# Quiz - コード変更内容についての復習

git diff でブランチの変更コードを確認し、ユーザーがコードを1行1行理解しているかチェックするクイズを行う。

## 手順

1. `git log --oneline -5` と `git diff main...HEAD` または `git diff release-candidate...HEAD` などで変更内容を確認する
2. 変更されたコードの中からランダムで1箇所を選ぶ
3. その箇所のコードを引用して「このコードは何をしていますか？」と日本語で質問する
4. 私が答えたら正誤・補足を教える
5. その Q&A を後述のフォーマットでログファイルに追記する
6. 「次の質問に進みますか？」とAskUserQuestionツールを使って聞く

質問は具体的なコード片（メソッド名、CSS property、関数など）を引用してください。
diff がない場合は「変更されたコードが見つかりません」と教えてください。

## ログの記録

各 Q&A のやりとりを `~/.claude/quiz-logs/YYYY-MM-DD.md`（その日の日付）に追記してください。

- ディレクトリが存在しなければ `mkdir -p ~/.claude/quiz-logs` で作成する
- ファイルが存在しなければ `Write` で新規作成、存在すれば `Edit` で末尾に追記する
- 1 セッションの最初の Q&A の前に `# YYYY-MM-DD のクイズ` という見出しを書く（既存ファイルなら不要）
- 各 Q&A は次のフォーマットで記録する：

```markdown
## HH:MM (branch: <現在のブランチ名>)

**Q:** （質問文）
\`\`\`<lang>
（引用したコード片）
\`\`\`

**A (私):** （ユーザーの回答をそのまま）

**評価:** （正誤・補足）
```

- 記録は手順 4 のあと、手順 6 の前に行う
- 私の回答が「わからない」「スキップ」などの場合もそのまま記録する
